import { useMemo, useState } from 'react'
import './App.css'

const initialJobs = [
  { id: 1, title: 'Senior Product Designer', company: 'Northstar Labs', location: 'New York, NY', type: 'Full-time', salary: '$140k - $175k', posted: '2d ago', category: 'Design', color: 'coral', description: 'Lead the design vision for tools that help ambitious teams do their best work. You will partner with product, engineering, and research to make complex workflows feel simple.', responsibilities: ['Own the end-to-end design process from discovery to delivery', 'Build thoughtful, scalable patterns with our design system', 'Present work clearly and invite useful critique'], requirements: ['5+ years of product design experience', 'A strong portfolio showing systems thinking', 'Excellent written and verbal communication'] },
  { id: 2, title: 'Frontend Engineer', company: 'Cinder & Co.', location: 'Remote / US', type: 'Full-time', salary: '$125k - $160k', posted: '3d ago', category: 'Engineering', color: 'blue', description: 'Join a small, senior team building the next generation of collaborative finance software. Your work will be visible, meaningful, and shipped quickly.', responsibilities: ['Build fast, accessible interfaces in React', 'Collaborate closely with design and backend engineers', 'Raise the bar for frontend quality and performance'], requirements: ['3+ years with React and TypeScript', 'Strong HTML, CSS, and accessibility fundamentals', 'A product mindset and curiosity'] },
  { id: 3, title: 'Brand Marketing Manager', company: 'Good Habit', location: 'Austin, TX', type: 'Full-time', salary: '$95k - $120k', posted: '5d ago', category: 'Marketing', color: 'yellow', description: 'Shape the story of a company making everyday wellbeing more delightful. This role blends strategic thinking with hands-on creative energy.', responsibilities: ['Develop campaigns that move people and metrics', 'Partner with creative teams on memorable launches', 'Turn customer insight into a clear brand voice'], requirements: ['4+ years of brand or growth marketing experience', 'Excellent copywriting skills', 'Experience in a fast-moving startup'] },
  { id: 4, title: 'Product Operations Intern', company: 'Orbit Health', location: 'Boston, MA', type: 'Internship', salary: '$24 - $28 / hr', posted: '1w ago', category: 'Operations', color: 'mint', description: 'Learn how a modern healthcare company turns great ideas into products people trust. You will have a real seat at the table from day one.', responsibilities: ['Support product planning and team rituals', 'Create clear documentation and project updates', 'Research opportunities to improve our workflows'], requirements: ['Currently pursuing a degree in business or a related field', 'Highly organized and eager to learn', 'Strong attention to detail'] },
]

function App() {
  const [jobs, setJobs] = useState(initialJobs)
  const [query, setQuery] = useState('')
  const [location, setLocation] = useState('')
  const [activeView, setActiveView] = useState('jobs')
  const [selectedJob, setSelectedJob] = useState(null)
  const [saved, setSaved] = useState([])
  const [applyJob, setApplyJob] = useState(null)
  const [toast, setToast] = useState('')
  const filteredJobs = useMemo(() => jobs.filter((job) => `${job.title} ${job.company} ${job.category}`.toLowerCase().includes(query.toLowerCase()) && job.location.toLowerCase().includes(location.toLowerCase())), [jobs, query, location])
  const toggleSaved = (id) => setSaved((current) => current.includes(id) ? current.filter((item) => item !== id) : [...current, id])
  const notify = (message) => { setToast(message); window.setTimeout(() => setToast(''), 2600) }
  const postJob = (event) => { event.preventDefault(); const form = new FormData(event.currentTarget); const newJob = { id: Date.now(), title: form.get('title'), company: 'Your company', location: form.get('location'), type: form.get('type'), salary: form.get('salary') || 'Salary not listed', posted: 'just now', category: 'New role', color: 'blue', description: 'A new opportunity from an ambitious team. Bring your ideas, craft, and curiosity to work that matters.', responsibilities: ['Make an impact from day one', 'Collaborate with a thoughtful team', 'Help us build what comes next'], requirements: ['Relevant experience or a strong point of view', 'Clear communication', 'A bias toward action'] }; setJobs((current) => [newJob, ...current]); event.currentTarget.reset(); notify('Job posted successfully') }
  return (
    <div className="app-shell">
      <nav className="nav"><a className="logo" href="#top" onClick={() => setActiveView('jobs')}><span className="logo-mark">J</span> jobly</a><div className="nav-links"><button className={activeView === 'jobs' ? 'active' : ''} onClick={() => setActiveView('jobs')}>Find jobs</button><button className={activeView === 'companies' ? 'active' : ''} onClick={() => setActiveView('companies')}>Companies</button><button className={activeView === 'about' ? 'active' : ''} onClick={() => setActiveView('about')}>About us</button></div><div className="nav-actions"><button className="employer-link" onClick={() => setActiveView('employer')}>For employers</button><button className="login" onClick={() => notify('Login is ready to connect')}>Log in</button><button className="signup" onClick={() => notify('Sign up flow opened')}>Sign up <span>↗</span></button></div></nav>
      {activeView === 'employer' ? <main className="employer-page"><div className="section-kicker">EMPLOYER STUDIO</div><h1>Build your next<br /><em>great team.</em></h1><p className="lead">Share your mission with the people who can move it forward.</p><form className="post-form" onSubmit={postJob}><div className="form-head"><div><span className="step">01 / 01</span><h2>Post a new role</h2></div><span className="form-note">Takes about 2 minutes</span></div><label>Job title<input name="title" required placeholder="e.g. Senior Product Designer" /></label><div className="form-grid"><label>Location<input name="location" required placeholder="e.g. New York, NY" /></label><label>Job type<select name="type" defaultValue="Full-time"><option>Full-time</option><option>Part-time</option><option>Internship</option></select></label></div><label>Salary range <span className="optional">optional</span><input name="salary" placeholder="e.g. $100k - $140k" /></label><button className="primary" type="submit">Publish role <span>↗</span></button></form></main> : activeView === 'companies' ? <main className="info-page"><div className="section-kicker">THE JOBLY NETWORK</div><h1>Meet the teams<br /><em>doing good work.</em></h1><p className="lead">Explore companies building useful things, thoughtful cultures, and careers with room to grow.</p><div className="company-grid">{[...new Set(jobs.map((job) => job.company))].map((company, index) => <button className="company-tile" key={company} onClick={() => { setQuery(company); setActiveView('jobs') }}><div className={`company-icon ${jobs[index % jobs.length].color}`}>{company.charAt(0)}</div><h2>{company}</h2><p>{jobs.find((job) => job.company === company)?.category} roles · View openings ↗</p></button>)}</div></main> : activeView === 'about' ? <main className="info-page about-page"><div className="section-kicker">A NOTE FROM JOBLY</div><h1>Work is better<br /><em>when it fits.</em></h1><p className="lead">Jobly helps people find work that feels aligned and helps companies build teams they are proud of.</p><div className="about-columns"><div><span className="about-number">01</span><h2>For people</h2><p>Search with more context. Discover roles, teams, and missions that match the way you want to work.</p></div><div><span className="about-number">02</span><h2>For companies</h2><p>Tell a better story about your work and meet candidates who are excited to build with you.</p></div></div><button className="primary" onClick={() => setActiveView('jobs')}>Explore open roles <span>↗</span></button></main> : <main id="top"><section className="hero-section"><div className="hero-copy"><div className="eyebrow">THE BETTER WAY TO WORK</div><h1>Do work<br /><em>that matters.</em></h1><p>Find the role that fits your life, your values, and the version of you you're becoming.</p></div><div className="hero-art"><div className="hero-photo"></div><div className="floating-note note-one">✦ <strong>12,480</strong><small>open positions</small></div><div className="floating-note note-two"><span className="avatar-stack">● ● ●</span><strong>Join the good work</strong></div></div></section><section className="search-wrap"><div className="search-bar"><label><span>⌕</span><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Job title, keyword, or company" /></label><label><span>⌖</span><input value={location} onChange={(event) => setLocation(event.target.value)} placeholder="City, state, or remote" /></label><button className="search-button" onClick={() => document.querySelector('.jobs-section')?.scrollIntoView({ behavior: 'smooth' })}>Search jobs <span>↗</span></button></div><div className="quick-searches"><span>Popular:</span><button onClick={() => setQuery('Design')}>Design</button><button onClick={() => setQuery('Engineering')}>Engineering</button><button onClick={() => setQuery('Marketing')}>Marketing</button><button onClick={() => setLocation('Remote')}>Remote</button></div></section><section className="jobs-section"><div className="section-header"><div><div className="eyebrow">CURATED FOR YOU</div><h2>Explore open roles <span className="count">{filteredJobs.length}</span></h2></div><button className="view-all" onClick={() => { setQuery(''); setLocation('') }}>View all roles <span>↗</span></button></div><div className="jobs-layout"><aside><strong>Filter by</strong><button className="filter active">All roles <span>{jobs.length}</span></button><button className="filter">Full-time <span>3</span></button><button className="filter">Internship <span>1</span></button><hr /><strong>Workplace</strong><button className="filter">Remote</button><button className="filter">On-site</button><div className="save-spot"><span>♡</span><strong>Saved jobs</strong><p>Keep track of roles you love.</p><button onClick={() => notify(`${saved.length} saved job${saved.length === 1 ? '' : 's'}`)}>View saved <span>→</span></button></div></aside><div className="job-list">{filteredJobs.map((job) => <article className="job-card" key={job.id} onClick={() => setSelectedJob(job)}><div className={`company-icon ${job.color}`}>{job.company.charAt(0)}</div><div className="job-info"><div className="job-meta"><span>{job.category}</span><span>•</span><span>{job.posted}</span></div><h3>{job.title}</h3><p>{job.company} <span>·</span> {job.location}</p><div className="tags"><span>{job.type}</span><span>{job.salary}</span></div></div><button className={`heart ${saved.includes(job.id) ? 'saved' : ''}`} aria-label="Save job" onClick={(event) => { event.stopPropagation(); toggleSaved(job.id) }}>{saved.includes(job.id) ? '♥' : '♡'}</button><span className="card-arrow">↗</span></article>)}{filteredJobs.length === 0 && <div className="empty">No roles found. Try a broader search.</div>}</div></div></section></main>}
      <footer><a className="logo" href="#top"><span className="logo-mark">J</span> jobly</a><p>Good work starts with a good fit.</p><span>© 2024 jobly</span></footer>
      {selectedJob && <div className="modal-backdrop" onClick={() => setSelectedJob(null)}><div className="detail-modal" onClick={(event) => event.stopPropagation()}><button className="close" onClick={() => setSelectedJob(null)}>×</button><div className={`company-icon ${selectedJob.color}`}>{selectedJob.company.charAt(0)}</div><div className="eyebrow">{selectedJob.category} · {selectedJob.posted}</div><h2>{selectedJob.title}</h2><p className="detail-company">{selectedJob.company} · {selectedJob.location}</p><div className="tags"><span>{selectedJob.type}</span><span>{selectedJob.salary}</span></div><h3>About the role</h3><p>{selectedJob.description}</p><h3>What you'll do</h3><ul>{selectedJob.responsibilities.map((item) => <li key={item}>{item}</li>)}</ul><h3>What we're looking for</h3><ul>{selectedJob.requirements.map((item) => <li key={item}>{item}</li>)}</ul><button className="primary apply" onClick={() => { setApplyJob(selectedJob); setSelectedJob(null) }}>Apply for this role <span>↗</span></button></div></div>}
      {applyJob && <div className="modal-backdrop" onClick={() => setApplyJob(null)}><form className="apply-modal" onSubmit={(event) => { event.preventDefault(); setApplyJob(null); notify('Application sent. Good luck!') }} onClick={(event) => event.stopPropagation()}><button type="button" className="close" onClick={() => setApplyJob(null)}>×</button><div className="eyebrow">APPLYING TO {applyJob.company.toUpperCase()}</div><h2>{applyJob.title}</h2><label>Full name<input required placeholder="Your name" /></label><label>Email address<input required type="email" placeholder="you@example.com" /></label><label>Short note<textarea required placeholder="Tell them why you're a great fit..."></textarea></label><button className="primary" type="submit">Send application <span>↗</span></button></form></div>}
      {toast && <div className="toast">{toast} <span>✓</span></div>}
    </div>
  )
}
export default App
